# 変更・不具合報告

通常の不具合はGitHub Issuesで、OS、Python/Node.jsのバージョン、コミット、期待結果、実際の結果、匿名化した再現手順を知らせてください。APIキーや実データは添付しないでください。機密性のある報告はSECURITY.mdを参照してください。

変更前に仮想環境を作り、READMEに従って固定依存関係を導入します。変更後は `python -m pytest -q`、フロントエンドは `npm run lint` と `npm run build` を実行してください。実APIを使うテストを追加せずモックを使用します。依存更新時は両言語の脆弱性監査も実施します。

PRには具体的な変更内容、再現条件、検証結果、既存データへの影響を書いてください。フロントエンドを変更した場合は再ビルドしたdistも含めます。
